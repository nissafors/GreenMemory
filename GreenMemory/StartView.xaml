<UserControl x:Class="GreenMemory.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenMemory"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1066"
             Background="LightGreen">
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <local:AnimatedButton x:Name="te" Margin="10" ButtonImage="btplay.png" Click="quickstart" Text="QuickStart"></local:AnimatedButton>
            <local:AnimatedButton Margin="10" ButtonImage="btplay.png" Click="settings" Text="Start"></local:AnimatedButton>
        </StackPanel>
        <Grid>
            <Image Source="doot.gif" Stretch="Fill"></Image>
            <Label HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="72" Foreground="Red">DOOT DOOT mr Skeletal</Label>
        </Grid>
    </DockPanel>
</UserControl>
