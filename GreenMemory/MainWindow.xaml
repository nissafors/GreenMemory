<Window x:Class="GreenMemory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GreenMemory"
        Title="Memory" Height="600" Width="600"
        MinHeight="600" MinWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <local:PlayerView x:Name="playerOneView" Grid.Row="0" Grid.Column="1"/>
        <local:PlayerView x:Name="playerTwoView" Grid.Row="2" Grid.Column="1"/>
        
        <local:SettingsView x:Name="settings" Grid.Row="1" Grid.Column="1"/>

        <Grid Name="CardGrid" 
              Grid.Column="0" 
              Grid.Row="0" 
              Grid.RowSpan="3"/>
    </Grid>
</Window>
