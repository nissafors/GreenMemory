<Window x:Class="GreenMemory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GreenMemory"
        Title="Memory" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New"/>
                    <Separator/>
                    <MenuItem Header="E_xit"/>
                </MenuItem>
            </Menu>
        </DockPanel>
        <Grid Name="CardGrid" 
              Grid.Column="0" 
              Grid.Row="0" 
              Grid.RowSpan="2"/>

        <local:PlayerView x:Name="player1" Grid.Row="0" Grid.Column="1"/>
        <local:PlayerView x:Name="player2" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>
