<UserControl x:Class="GreenMemory.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenMemory"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1066">
    <Grid Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <local:PlayerView x:Name="playerOneView" Grid.Row="0" Grid.Column="1"/>
        <local:PlayerView x:Name="playerTwoView" Grid.Row="2" Grid.Column="1"/>

        <local:OptionsView x:Name="settings" Grid.Row="1" Grid.Column="1"/>
        <Viewbox Grid.Column="0" 
                 Grid.Row="0" 
                 Grid.RowSpan="3"
                 Stretch="Uniform"
                 Margin="10">
            <Grid Name="CardGrid"/>
        </Viewbox>
    </Grid>
</UserControl>
