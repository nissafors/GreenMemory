<UserControl x:Class="GreenMemory.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenMemory"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1066">
    <Grid Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <local:AnimatedButton  x:Name="btnBack" Margin="10,-91,0,91" ButtonImage="Game/Icons/3X/Back@3x.png" Click="backToSettings" />

        <local:PlayerView x:Name="playerOneView" changedName="playerOne_nameChanged"  Grid.Row="0" Grid.Column="2" Margin="10,60,0,10"/>
        <local:PlayerView x:Name="playerTwoView" changedName="playerTwo_nameChanged" Grid.Row="1" Grid.Column="2" Margin="10,60,0,10"/>

        <local:AnimatedButton Grid.Row="2" Grid.Column="2" ButtonImage="Game/Icons/3X/Settings@3x.png" Margin="65.5,30,-26,10" Click="openSettingsWindow"/>
        <Viewbox Grid.Column="1" 
                 Grid.Row="0" 
                 Grid.RowSpan="3"
                 Stretch="Uniform"
                 Margin="10"
                 Name="viewBox">
            <Grid Name="CardGrid"/>
        </Viewbox>
        <local:GameOverWindow x:Name="gameoverWin" ClickedRestart="restartGameClick" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="3" Visibility="Collapsed"/>
        <local:settingsWindow x:Name="settingsWin" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="3" Visibility="Collapsed"/>
    </Grid>
</UserControl>
