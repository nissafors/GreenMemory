<UserControl x:Class="GreenMemory.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreenMemory"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1066">
    <Grid Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        
        <local:AnimatedButton x:Name="btnBack" ButtonImage="Game/Icons/3X/Back@3x.png" Click="backToSettings"/>

        <local:PlayerView x:Name="playerOneView" Grid.Row="0" Grid.Column="2"/>
        <local:PlayerView x:Name="playerTwoView" Grid.Row="2" Grid.Column="2"/>

        <local:AnimatedButton x:Name="btnNewGame" Grid.Row="1" Grid.Column="2" ButtonImage="btplay.png" Margin="16,8,16,8"/>
        <Viewbox Grid.Column="1" 
                 Grid.Row="0" 
                 Grid.RowSpan="3"
                 Stretch="Uniform"
                 Margin="10"
                 Name="viewBox">
            <Grid Name="CardGrid"/>
        </Viewbox>
    </Grid>
</UserControl>
